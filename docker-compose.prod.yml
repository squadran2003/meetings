version: '3.8'

services:
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./frontend/dist:/app/static
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
